cargo init. inside mcos (installs new rust binary project , in dont have rust use this code {curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh})

if you mac dont have  Protocol Buffers Compiler (protoc) {brew install protobuf}
